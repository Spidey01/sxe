
buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.5.7'
    }
}

apply plugin: 'android-library'

group = 'com.spidey01.sxe.android.common'
// archivesBaseName = 'sxe-android-common'
version = '0.0.1-SNAPSHOT'
description = 'Platform library for Android; common code.'

dependencies {
    compile project(':core')
    releaseCompile project(':core')
    debugCompile project(':core')
}

android {
    // needed by android-library's :android:compileReleaseAidl task.
    buildToolsVersion "18.1.0"
    compileSdkVersion 10
    defaultConfig {
        // are these needed for a library project?
        // versionCode 1
        // versionName getVersionName()
        minSdkVersion 10
        // is this needed in a library project?
        //targetSdkVersion 17
    }
}

