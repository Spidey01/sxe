#ifndef SXE_CONFIG__H
#define SXE_CONFIG__H


/** Whether SxE was built with Boost. */
#cmakedefine01 SXE_HAVE_BOOST

/** Whether SxE was built with runtime Vulkan support. */
#cmakedefine01 SXE_HAVE_VULKAN

/** Whether SxE was built with runtime OpenGL support. */
#cmakedefine01 SXE_HAVE_OPENGL

#endif // SXE_CONFIG__H