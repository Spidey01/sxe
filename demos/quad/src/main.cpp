
int main(int argc, char* argv[])
{
    /* java:
        GameEngine g = PcConfiguration.setup(args, new QuadGame());
        g.start();
        g.mainLoop();
        g.stop();
    */
    return 0;
}

